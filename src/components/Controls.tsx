import React from "react";
import "./Controls.css";

const Controls = () => {
  return (
    <div className="bar">
      <button>Start</button>
      <button>Pause</button>
      <button>Step</button>
      <button>Reset</button>
    </div>
  );
};

export default Controls;
